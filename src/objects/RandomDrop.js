/*    <script src="src/objects/RandomDrop.js"></script>

class RandomDrop extends Phaser.Physics.Arcade.Sprite{
    constructor(scene, x, y) {
        super(scene, x, y, "piece")
        scene.add.existing(this)
        scene.physics.add.existing(this)

        this.setCollideWorldBounds(true)
        this.setBodySize(this.body.width,this.body.height+46);
       
        EventRandomDrop (player, RandomDrop, monster)
        {
            RandomDrop.disableBody(true, true);
            ui.gagne();
            this.EventRandomDrop=true;
            if( this.EventRandomDrop == true){
                switch (){
                    case "1":
                        Tableau.current.player.directionX=1;
                        break;
                    
                    case "2":
                        Tableau.current.player.directionX=1;
                        break;

                    case "3":
                        Tableau.current.player.directionX=1;
                        break;

            }  
        }
    }
}*/